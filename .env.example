MONGODB_URI=mongodb://nexio:nexio@localhost:27017/nexio_workflow?authSource=admin
PORT=8080
# O perfil dev liga o GraphiQL e a criacao automatica de indices; fora dele ambos ficam desligados
SPRING_PROFILES_ACTIVE=dev
# GRAPHIQL_ENABLED e opt-in explicito: sem o perfil dev, o padrao e false em qualquer ambiente
GRAPHIQL_ENABLED=true
# Producao (Render): sem SPRING_PROFILES_ACTIVE=dev e sem GRAPHIQL_ENABLED
# MONGODB_URI=<Atlas URI>
# NEXIO_MONGODB_ENSURE_INDEXES=true

# Teto de tempo total de uma execucao. Nao interrompe um no em andamento: e conferido antes de
# comecar cada no, entao o pior caso real e este teto mais a duracao do ultimo no.
# NEXIO_ENGINE_MAX_EXECUTION_DURATION=30s

# Permite que um no HTTP chame endereco http:// sem TLS. Fica false fora de desenvolvimento --
# ligar isto manda o cabecalho Authorization do no em texto claro pela rede.
# NEXIO_HTTP_ALLOW_INSECURE=false
